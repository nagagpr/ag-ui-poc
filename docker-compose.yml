services:
  backend:
    build: ./backend
    container_name: ag-ui-poc-backend
    ports:
      - "8000:8000"

  frontend:
    build: ./frontend
    container_name: ag-ui-poc-frontend
    ports:
      - "3000:80"
    depends_on:
      - backend
